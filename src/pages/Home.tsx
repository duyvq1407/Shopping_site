import React from 'react'
import { IProduct } from '../types/product'

type HomeProps = {
  products: IProduct[]
}

const Home = (props: HomeProps) => {
  return (
    <div>Home</div>
  )
}

export default Home